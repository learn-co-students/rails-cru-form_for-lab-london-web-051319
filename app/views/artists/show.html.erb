<p id="notice"><%= notice %></p>

<div class="">
  <strong>Name:</strong>
  <h1><%= @artist.name %></h1>
</div>

<p>
  <strong>Bio:</strong>
  <%= @artist.bio %>
</p>

<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
