<h1>Song Profile:</h1>

<h1><%=@song.name%></h1>

<h2><%=link_to @song.genre.name, genre_path(@song.genre_id) %></h2>

<h2><%=link_to @song.artist.name, artist_path(@song.artist_id) %></h2>




<p><%=link_to "Edit this Song", edit_song_path(@song)%></p>
